eureka:
  client:
    enabled: false

spring:
  application:
    name: gt-dictionary
  cloud:
    config:
      enabled: false
  main.allow-bean-definition-overriding: true

spring.jpa.properties.javax.persistence.schema-generation.scripts:
  action: create
  create-target: ../doc/db/scripts/mysql_scheme.sql
